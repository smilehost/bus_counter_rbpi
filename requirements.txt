opencv-python>=4.8.0
pandas>=2.0.0
ultralytics>=8.0.0
cvzone>=1.5.0
numpy>=1.21.0,<2.0.0

# PyTorch - Platform specific installation
# For Windows with CUDA GPU:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# For Windows CPU only:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# For automatic detection (current):
torch>=2.0.0
torchvision>=0.15.0
cython>=0.29.0
filterpy>=1.4.5
scikit-learn>=1.0.0
motmetrics>=1.2.0
lap>=0.5.0
scipy>=1.7.0
deepface>=0.0.79
tensorflow>=2.10.0
mtcnn>=0.1.0
retina-face>=0.0.12
psutil>=5.8.0


# Raspberry Pi 5 camera support
# For Pi Camera Module (install via apt on Raspberry Pi OS)
# picamera2 (system package)
# libcamera (system package)

# Enhanced camera module dependencies
psutil>=5.8.0  # Already included, but needed for camera monitoring

# Performance monitoring for Pi5
gpustat>=1.1.1


# ============================================================================
# Hailo AI Accelerator Support (OPTIONAL)
# ============================================================================
# The following dependencies are optional and only required if you want to use
# Hailo 26 AI accelerators for inference. The code will gracefully fall back to
# YOLO/CUDA/CPU if these dependencies are not installed.
#
# To install Hailo dependencies:
# pip install -r requirements.txt
#
# Note: Hailo runtime requires proper driver installation on your system.
# Visit https://hailo.ai/developer-zone/ for installation guides.
#
# hailort: Hailo Runtime - provides Python bindings for Hailo hardware
#          Required for running inference on Hailo 26 accelerators
#          Version 4.18.0+ recommended for best compatibility
hailort>=4.18.0

# hailo-model-zoo: Collection of pre-optimized models for Hailo accelerators
#                   Optional - provides access to pre-compiled HEF models
#                   Useful for testing and development
# hailo-model-zoo>=2.0.0