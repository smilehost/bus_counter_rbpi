opencv-python>=4.8.0
pandas>=2.0.0
ultralytics>=8.0.0
cvzone>=1.5.0
numpy>=1.21.0,<2.0.0
torch>=2.0.0
torchvision>=0.15.0
cython>=0.29.0
filterpy>=1.4.5
scikit-learn>=1.0.0
motmetrics>=1.2.0
lap>=0.5.0
scipy>=1.7.0
deepface>=0.0.79
tensorflow>=2.10.0
mtcnn>=0.1.0
retina-face>=0.0.12
psutil>=5.8.0

# HAILO AI Accelerator dependencies
# Optional: for model compilation and optimization
# hailo-sdk>=4.17.0

# Raspberry Pi camera support with GStreamer
# Note: System packages need to be installed separately:
# sudo apt-get update
# sudo apt-get install python3-gi python3-gst-1.0 gstreamer1.0-plugins-base gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-tools gstreamer1.0-libav
# sudo apt-get install libgstreamer-plugins-base1.0-dev libgstreamer1.0-dev
# For Raspberry Pi camera specifically (Pi 5 uses libcamera):
# sudo apt-get install gstreamer1.0-libcamera
# For older Pi models:
# sudo apt-get install gstreamer1.0-rpicamsrc

# Performance monitoring for Pi5
gpustat>=1.1.1