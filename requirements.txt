opencv-python>=4.8.0
pandas>=2.0.0
ultralytics>=8.0.0
cvzone>=1.5.0
numpy>=1.21.0,<2.0.0

# PyTorch - Platform specific installation
# For Windows with CUDA GPU:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# For Windows CPU only:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# For automatic detection (current):
torch>=2.0.0
torchvision>=0.15.0
cython>=0.29.0
filterpy>=1.4.5
scikit-learn>=1.0.0
motmetrics>=1.2.0
lap>=0.5.0
scipy>=1.7.0
deepface>=0.0.79
tensorflow>=2.10.0
mtcnn>=0.1.0
retina-face>=0.0.12
psutil>=5.8.0


# Raspberry Pi 5 camera support
# For Pi Camera Module (install via apt on Raspberry Pi OS)
# picamera2 (system package)
# libcamera (system package)

# Enhanced camera module dependencies
psutil>=5.8.0  # Already included, but needed for camera monitoring

# Performance monitoring for Pi5
gpustat>=1.1.1